#include <stdio.h>

void Crt_arr(int n, int a[])
{
    for (int i = 0; i < n; i++)
    {
        printf("Enter element %d: ", i + 1);
        scanf("%d", &a[i]);
    }
}

void Trav_arr(int a[])
{
    for(int i=0; i < )
}

int main()
{
    int n, arr[100];

    printf("Enter the no. of elements: ");
    scanf("%d",&n);
    Crt_arr(n, arr);
    

    return 0;
}